# Root environment variables
# Copy to .env and fill in values
# .env is gitignored - never commit real values!

# ============================================================================
# Cloudflare Credentials (for Alchemy infrastructure deployment)
# ============================================================================
# Option 1: Use `pnpm wrangler login` (recommended - no env vars needed)
# Option 2: Set these variables for CI/CD or headless deployment

# Get from Cloudflare Dashboard -> My Profile -> API Tokens
# Create a token with "Edit Cloudflare Workers" permissions
CLOUDFLARE_API_TOKEN=your-api-token

# Get from Cloudflare Dashboard -> Account Home (in URL or sidebar)
CLOUDFLARE_ACCOUNT_ID=your-account-id
