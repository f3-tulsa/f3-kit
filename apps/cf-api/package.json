{
  "name": "cf-api",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev --port 8787",
    "deploy": "wrangler deploy",
    "build": "tsc -p tsconfig.build.json",
    "d1:migrate:local": "wrangler d1 migrations apply f3-db --local",
    "d1:migrate:remote": "wrangler d1 migrations apply f3-db --remote"
  },
  "dependencies": {
    "hono": "^4.5.0",
    "f3-api": "workspace:*",
    "repo-d1": "workspace:*"
  },
  "devDependencies": {
    "typescript": "^5.5.0",
    "@cloudflare/workers-types": "^4.20251001.0",
    "wrangler": "^3.100.0"
  }
}